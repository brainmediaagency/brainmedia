rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Storage unused — files on Drive; reopen with path-scoped rules if Storage is reintroduced.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
