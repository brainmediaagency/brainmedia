rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Storage unused (Spark) — files live on Google Drive via Apps Script.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
