/**
 * Master switch for mesai / attendance / shift UI surfaces.
 * Keep services, hooks, Firestore rules, and source files intact —
 * flip to `true` to re-enable nav tabs, widgets, and related screens.
 */
export const SHOW_MESAI_UI = false
